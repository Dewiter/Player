import Input from './Inputs/Inputs';

const Player = ({ queue }) => {
  return (
    <div className='container container-out player'>
      <h1>placeholder</h1>
      <Input queue={queue} />
    </div>
  );
};

export default Player;
